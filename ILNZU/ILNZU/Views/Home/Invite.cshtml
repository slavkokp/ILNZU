@{
    ViewData["Title"] = "My invites";
}

<div class="container">
    <div class="chat-frame">
        <div class="d-flex flex-column">
            <h2>Invite list</h2>
            @foreach (var list in ViewBag.Invites)
            {
                <p>
                    <div class="p-2">
                        <p>@list.Value @list.Key</p>

                    </div>

                </p>
            }
        </div>
    </div>
</div>